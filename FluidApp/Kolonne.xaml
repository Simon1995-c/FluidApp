<Page
    Name="thisPage"
    x:Class="FluidApp.Kolonne"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluidApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <local:KolonneViewModel/>
    </Page.DataContext>
    <Grid>
        <Grid VerticalAlignment="Top" Background="DarkCyan" Padding="15 25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Button Command="{Binding TilbageCommand}" Content="Tilbage" Foreground="white"></Button>
            </Grid>
            <Grid Grid.Column="1" HorizontalAlignment="Center">
                <TextBlock FontSize="26" Foreground="White" Text="Kolonne 2"></TextBlock>
            </Grid>
            <Grid Grid.Column="2" HorizontalAlignment="Right">
                <!--Afhængig af admin rolle-->
                <Button Content="Opret nyt skema" Visibility="{Binding}" Command="{Binding NavigerOpretSkemaCommand}" Foreground="White"></Button>
            </Grid>
        </Grid>
        
        <StackPanel Orientation="Vertical" VerticalAlignment="Top" Padding="50" Margin="0 50 0 0">
            <Grid Padding="10">
                <!--2x2 grid-->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid Grid.Column="0" Grid.Row="0">
                    <TextBlock Text="Alle skemaer" FontWeight="Bold"></TextBlock>
                </Grid>
                <Grid Grid.Column="1" Grid.Row="0" HorizontalAlignment="Right">
                    <StackPanel Orientation="Horizontal">
                        <Border CornerRadius="9">
                            <Button Content="Sortér" Command="{Binding SorterCommand}"></Button>
                        </Border>
                        <Border CornerRadius="9">
                            <Button Content="Genindlæs" Command="{Binding GenindlæsCommand}"></Button>
                        </Border>
                    </StackPanel>
                </Grid>
                <!--To ListViews for ellers virkede horizontalalignment ikke-->
                <Grid Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" Margin="0 10 0 0">
                    <ListView ItemsSource="{Binding KolonneListe}" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Margin="10" Text="{Binding ProcessordreNr}"></TextBlock>
                                    <TextBlock Margin="10" Text="{Binding FærdigvareNr}"></TextBlock>
                                    <TextBlock Margin="10" Text="{Binding FærdigvareNavn}"></TextBlock>
                                    <TextBlock Margin="10">
                                        <Run Text="{Binding Dato.Day}"></Run><Run Text="-"></Run><Run Text="{Binding Dato.Month}"></Run><Run Text="-"></Run><Run Text="{Binding Dato.Year}"></Run>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
                <Grid Grid.Column="1" Grid.Row="1" HorizontalAlignment="Right" Margin="0 10 0 0">
                    <ListView ItemsSource="{Binding KolonneListe}" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border CornerRadius="9" Margin="0 5 0 0">
                                    <Button Content="Se mere" Command="{Binding ElementName=thisPage, Path=DataContext.SeMere}"></Button>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
